The array must have all the elements less than key first, then all the elements equal to key,
and then all the elements greater than key. Note that the relative ordering of all elements less
than key and all elements greater than key does not matter.
